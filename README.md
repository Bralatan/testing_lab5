Виконав Бралатан Р.А., б17д_122-А, варіант №2

У програмі були реалізовані наступні функції та класи:
У класі Programe організовано ввід даних з клавіатури та перевірка їх на відповідність заданим умовам (має бути число не менше 1.39 і не більше 67.117).
Для цієї перевірки у класі реалізовані дескриптори getter та setter. Якщо введене число відповідає усім потрібним умовам воно передається наступні функції даного класу:

Функція equation1 повертає обраховане число за такою формулою y = self.x**4*1.554+self.x**3*3.859-self.x**2*2.458+self.x*1.851

Функція equation2 повертає значення після обрахування наступної формули y = self.x**3*2.498-self.x**2*2.055+self.x*5.72

Функція equation3 повертає значення після обрахування наступної формули y = self.x**2*1.948+self.x*3.572

Функція equation4 повертає значення після обрахування наступної формули y = self.x*4.314
